# API

Hier entsteht die Dokumentation für die öffentlichen Endpunkte der API von Runicroots.
Es wird zusätzlich zu den Endpunkten für die Server-Funktionalität auch öffentliche Endpunkte geben, die für Entwickler zur Verfügung
stehen. Allerdings wird es für diese ein Rate-Limit geben.
